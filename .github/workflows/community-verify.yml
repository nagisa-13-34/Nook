name: Community 2 verification

on:
  pull_request:
    branches:
      - main

permissions:
  contents: read

concurrency:
  group: community-2-verification-${{ github.ref }}
  cancel-in-progress: true

jobs:
  verify:
    runs-on: ubuntu-latest
    steps:
      - name: Temporary no-op
        run: echo "Temporarily disabled while global event verification avoids migrations."
