/*
 * SPDX-FileCopyrightText: syuilo and misskey-project
 * SPDX-License-Identifier: AGPL-3.0-only
 */
import { Inject, Injectable } from '@nestjs/common'; import { DataSource } from 'typeorm'; import { DI } from '@/di-symbols.js'; import { Endpoint } from '@/server/api/endpoint-base.js'; import { requireNookCommunityMember, NookCommunityAccessError } from '@/nook/community/access.js'; import { purgeNookTranslationCache } from '@/nook/translation/NookTranslationService.js'; import { ApiError } from '../../../../error.js';
export const meta = { tags: ['channels'], requireCredential: true, kind: 'write:channels', errors: { forbidden: { message: 'You cannot delete this message.', code: 'FORBIDDEN', id: '8150d32b-4964-4efa-a925-1a3a8d0df838' }, noSuchMessage: { message: 'No such message.', code: 'NO_SUCH_MESSAGE', id: 'e92eb67b-24fe-40ee-88f7-296389641d21' } } } as const;
export const paramDef = { type: 'object', properties: { communityId: { type: 'string', format: 'misskey:id' }, messageId: { type: 'string', format: 'misskey:id' } }, required: ['communityId', 'messageId'] } as const;
@Injectable() export default class extends Endpoint<typeof meta, typeof paramDef> { constructor(@Inject(DI.db) private db: DataSource) { super(meta, paramDef, async (ps, me) => { let membership; try { membership = await requireNookCommunityMember(this.db, ps.communityId, me.id); } catch (error) { if (error instanceof NookCommunityAccessError) throw new ApiError(meta.errors.forbidden); throw error; } const rows = await this.db.query<Array<{ userId: string | null }>>('SELECT "userId" FROM "nook_community_message" WHERE "communityId" = $1 AND "id" = $2 AND "deletedAt" IS NULL LIMIT 1', [ps.communityId, ps.messageId]); const message = rows[0]; if (message == null) throw new ApiError(meta.errors.noSuchMessage); if (message.userId !== me.id && !membership.permissions.has('*') && !membership.permissions.has('members.manage')) throw new ApiError(meta.errors.forbidden); await this.db.query('UPDATE "nook_community_message" SET "deletedAt" = now(), "body" = \'\' WHERE "id" = $1', [ps.messageId]); await purgeNookTranslationCache(this.db, 'communityMessage', ps.messageId); }); } }
