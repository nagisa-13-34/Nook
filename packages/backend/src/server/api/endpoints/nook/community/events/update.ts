/*
 * SPDX-FileCopyrightText: syuilo and misskey-project
 * SPDX-License-Identifier: AGPL-3.0-only
 */
import { Inject, Injectable } from '@nestjs/common'; import { DataSource } from 'typeorm'; import { DI } from '@/di-symbols.js'; import { Endpoint } from '@/server/api/endpoint-base.js'; import { requireNookCommunityPermission, NookCommunityAccessError } from '@/nook/community/access.js'; import { purgeNookTranslationCache } from '@/nook/translation/NookTranslationService.js'; import { ApiError } from '../../../../error.js';
export const meta = { tags: ['channels'], requireCredential: true, kind: 'write:channels', errors: { forbidden: { message: 'You cannot manage events.', code: 'FORBIDDEN', id: '14b268cb-937d-4899-af62-8d4b2426bbb1' }, noSuchEvent: { message: 'No such event.', code: 'NO_SUCH_EVENT', id: 'c4d0491a-5281-4878-ae03-ab498d34f6a5' }, invalidSchedule: { message: 'Invalid event schedule.', code: 'INVALID_SCHEDULE', id: '84f8e3c2-b216-405d-ac05-d989931db546' } } } as const;
export const paramDef = { type: 'object', properties: { communityId: { type: 'string', format: 'misskey:id' }, eventId: { type: 'string', format: 'misskey:id' }, title: { type: 'string', minLength: 1, maxLength: 160, nullable: true }, description: { type: 'string', maxLength: 12000, nullable: true }, location: { type: 'string', maxLength: 256, nullable: true }, startsAt: { type: 'string', format: 'date-time', nullable: true }, endsAt: { type: 'string', format: 'date-time', nullable: true }, maxAttendees: { type: 'integer', minimum: 1, maximum: 100000, nullable: true }, cancelled: { type: 'boolean', nullable: true } }, required: ['communityId', 'eventId'] } as const;
@Injectable() export default class extends Endpoint<typeof meta, typeof paramDef> { constructor(@Inject(DI.db) private db: DataSource) { super(meta, paramDef, async (ps, me) => { try { await requireNookCommunityPermission(this.db, ps.communityId, me.id, 'events.manage'); } catch (error) { if (error instanceof NookCommunityAccessError) throw new ApiError(meta.errors.forbidden); throw error; } const current = await this.db.query<Array<{ startsAt: Date; endsAt: Date | null }>>('SELECT "startsAt", "endsAt" FROM "nook_community_event" WHERE "communityId"=$1 AND "id"=$2 LIMIT 1', [ps.communityId, ps.eventId]); if (current[0] == null) throw new ApiError(meta.errors.noSuchEvent); const startsAt = ps.startsAt == null ? new Date(current[0].startsAt) : new Date(ps.startsAt); const endsAt = ps.endsAt === undefined ? current[0].endsAt : ps.endsAt == null ? null : new Date(ps.endsAt); if (endsAt != null && new Date(endsAt) <= startsAt) throw new ApiError(meta.errors.invalidSchedule); await this.db.query('UPDATE "nook_community_event" SET "title"=COALESCE($3,"title"), "description"=CASE WHEN $4::boolean THEN $5 ELSE "description" END, "location"=CASE WHEN $6::boolean THEN $7 ELSE "location" END, "startsAt"=$8, "endsAt"=$9, "maxAttendees"=CASE WHEN $10::boolean THEN $11 ELSE "maxAttendees" END, "cancelledAt"=CASE WHEN $12::boolean IS NULL THEN "cancelledAt" WHEN $12 THEN COALESCE("cancelledAt",now()) ELSE NULL END, "updatedAt"=now() WHERE "communityId"=$1 AND "id"=$2', [ps.communityId, ps.eventId, ps.title ?? null, ps.description !== undefined, ps.description ?? null, ps.location !== undefined, ps.location ?? null, startsAt, endsAt, ps.maxAttendees !== undefined, ps.maxAttendees ?? null, ps.cancelled ?? null]); if (ps.description !== undefined) await purgeNookTranslationCache(this.db, 'communityEvent', ps.eventId); }); } }
