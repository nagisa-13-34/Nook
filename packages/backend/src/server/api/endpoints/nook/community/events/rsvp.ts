/*
 * SPDX-FileCopyrightText: syuilo and misskey-project
 * SPDX-License-Identifier: AGPL-3.0-only
 */
import { Inject, Injectable } from '@nestjs/common'; import { DataSource } from 'typeorm'; import { DI } from '@/di-symbols.js'; import { Endpoint } from '@/server/api/endpoint-base.js'; import { requireNookCommunityMember, NookCommunityAccessError } from '@/nook/community/access.js'; import { setNookCommunityEventRsvp } from '@/nook/community/events.js'; import { ApiError } from '../../../../error.js';
export const meta = { tags: ['channels'], requireCredential: true, kind: 'write:channels', errors: { forbidden: { message: 'You must be a community member.', code: 'FORBIDDEN', id: 'bd9d7e7e-ec90-42d4-90f9-2acae8b1d782' }, eventUnavailable: { message: 'The event is unavailable.', code: 'EVENT_UNAVAILABLE', id: 'ca6dcee8-0c76-49d2-b576-1475106d1f92' }, eventFull: { message: 'The event is full.', code: 'EVENT_FULL', id: 'f534712f-2961-4c30-829d-0e33f23e522e' } } } as const;
export const paramDef = { type: 'object', properties: { communityId: { type: 'string', format: 'misskey:id' }, eventId: { type: 'string', format: 'misskey:id' }, response: { type: 'string', enum: ['going','interested','not_going'] } }, required: ['communityId','eventId','response'] } as const;
@Injectable() export default class extends Endpoint<typeof meta, typeof paramDef> { constructor(@Inject(DI.db) private db: DataSource) { super(meta, paramDef, async (ps, me) => { try { await requireNookCommunityMember(this.db, ps.communityId, me.id); } catch (error) { if (error instanceof NookCommunityAccessError) throw new ApiError(meta.errors.forbidden); throw error; } const eventRows = await this.db.query<Array<{ id: string }>>('SELECT "id" FROM "nook_community_event" WHERE "communityId"=$1 AND "id"=$2 LIMIT 1', [ps.communityId, ps.eventId]); if (eventRows[0] == null) throw new ApiError(meta.errors.eventUnavailable); try { await setNookCommunityEventRsvp(this.db, ps.eventId, me.id, ps.response); } catch (error) { if (error instanceof Error && error.message === 'EVENT_FULL') throw new ApiError(meta.errors.eventFull); if (error instanceof Error && error.message === 'EVENT_UNAVAILABLE') throw new ApiError(meta.errors.eventUnavailable); throw error; } }); } }
