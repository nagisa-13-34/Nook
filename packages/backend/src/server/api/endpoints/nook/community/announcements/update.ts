/*
 * SPDX-FileCopyrightText: syuilo and misskey-project
 * SPDX-License-Identifier: AGPL-3.0-only
 */
import { Inject, Injectable } from '@nestjs/common'; import { DataSource } from 'typeorm'; import { DI } from '@/di-symbols.js'; import { Endpoint } from '@/server/api/endpoint-base.js'; import { requireNookCommunityPermission, NookCommunityAccessError } from '@/nook/community/access.js'; import { purgeNookTranslationCache } from '@/nook/translation/NookTranslationService.js'; import { ApiError } from '../../../../error.js';
export const meta = { tags: ['channels'], requireCredential: true, kind: 'write:channels', errors: { forbidden: { message: 'You cannot manage announcements.', code: 'FORBIDDEN', id: '31f85b01-caa1-4bfa-b20d-0fd78791e397' }, noSuchAnnouncement: { message: 'No such announcement.', code: 'NO_SUCH_ANNOUNCEMENT', id: '9af97249-2563-423e-9419-52dc7b7ecafb' } } } as const;
export const paramDef = { type: 'object', properties: { communityId: { type: 'string', format: 'misskey:id' }, announcementId: { type: 'string', format: 'misskey:id' }, title: { type: 'string', minLength: 1, maxLength: 160, nullable: true }, body: { type: 'string', minLength: 1, maxLength: 12000, nullable: true }, important: { type: 'boolean', nullable: true }, expiresAt: { type: 'string', format: 'date-time', nullable: true } }, required: ['communityId', 'announcementId'] } as const;
@Injectable() export default class extends Endpoint<typeof meta, typeof paramDef> { constructor(@Inject(DI.db) private db: DataSource) { super(meta, paramDef, async (ps, me) => { try { await requireNookCommunityPermission(this.db, ps.communityId, me.id, 'announcements.manage'); } catch (error) { if (error instanceof NookCommunityAccessError) throw new ApiError(meta.errors.forbidden); throw error; } const rows = await this.db.query<Array<{ id: string }>>(`UPDATE "nook_community_announcement" SET "title"=COALESCE($3,"title"), "body"=COALESCE($4,"body"), "important"=COALESCE($5,"important"), "expiresAt"=CASE WHEN $6::boolean THEN $7 ELSE "expiresAt" END, "updatedAt"=now() WHERE "communityId"=$1 AND "id"=$2 RETURNING "id"`, [ps.communityId, ps.announcementId, ps.title ?? null, ps.body ?? null, ps.important ?? null, ps.expiresAt !== undefined, ps.expiresAt == null ? null : new Date(ps.expiresAt)]); if (rows[0] == null) throw new ApiError(meta.errors.noSuchAnnouncement); if (ps.body != null) await purgeNookTranslationCache(this.db, 'communityAnnouncement', ps.announcementId); }); } }
