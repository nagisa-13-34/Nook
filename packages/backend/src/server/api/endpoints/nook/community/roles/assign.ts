/*
 * SPDX-FileCopyrightText: syuilo and misskey-project
 * SPDX-License-Identifier: AGPL-3.0-only
 */
import { Inject, Injectable } from '@nestjs/common'; import { DataSource } from 'typeorm'; import { DI } from '@/di-symbols.js'; import { Endpoint } from '@/server/api/endpoint-base.js'; import { requireNookCommunityPermission, NookCommunityAccessError } from '@/nook/community/access.js'; import { assignNookCommunityRole, unassignNookCommunityRole, NookCommunityRoleError } from '@/nook/community/roles.js'; import { ApiError } from '../../../../error.js';
export const meta = { tags: ['channels'], requireCredential: true, kind: 'write:channels', errors: { forbidden: { message: 'You cannot manage roles.', code: 'FORBIDDEN', id: '124fd07a-4cf8-43b9-97a9-3a3df4c59375' }, noSuchRole: { message: 'No such role.', code: 'NO_SUCH_ROLE', id: '9c41e152-0abf-43d9-9207-e1f503674b02' } } } as const;
export const paramDef = { type: 'object', properties: { communityId: { type: 'string', format: 'misskey:id' }, userId: { type: 'string', format: 'misskey:id' }, roleId: { type: 'string', format: 'misskey:id' }, assigned: { type: 'boolean' } }, required: ['communityId', 'userId', 'roleId', 'assigned'] } as const;
@Injectable() export default class extends Endpoint<typeof meta, typeof paramDef> { constructor(@Inject(DI.db) private db: DataSource) { super(meta, paramDef, async (ps, me) => { try { await requireNookCommunityPermission(this.db, ps.communityId, me.id, 'roles.manage'); if (ps.assigned) await assignNookCommunityRole(this.db, ps.communityId, ps.userId, ps.roleId); else await unassignNookCommunityRole(this.db, ps.communityId, ps.userId, ps.roleId); } catch (error) { if (error instanceof NookCommunityAccessError) throw new ApiError(meta.errors.forbidden); if (error instanceof NookCommunityRoleError) throw new ApiError(meta.errors.noSuchRole); throw error; } }); } }
