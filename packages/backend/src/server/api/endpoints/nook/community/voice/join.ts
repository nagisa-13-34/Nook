/*
 * SPDX-FileCopyrightText: syuilo and misskey-project
 * SPDX-License-Identifier: AGPL-3.0-only
 */
import { Inject, Injectable } from '@nestjs/common'; import { DataSource } from 'typeorm'; import { DI } from '@/di-symbols.js'; import { Endpoint } from '@/server/api/endpoint-base.js'; import { joinNookCommunityVoice, NookCommunityVoiceError } from '@/nook/community/voice.js'; import { NookCommunityAccessError } from '@/nook/community/access.js'; import { NookCommunityChannelError } from '@/nook/community/channels.js'; import { ApiError } from '../../../../error.js';
export const meta = { tags: ['channels'], requireCredential: true, kind: 'write:channels', res: { type: 'object', optional: false, nullable: false, properties: { sessionId: { type: 'string' }, peers: { type: 'array', items: { type: 'string' } }, speakingPeerIds: { type: 'array', items: { type: 'string' } }, canSpeak: { type: 'boolean' }, iceServersJson: { type: 'string' }, iceTransportPolicy: { type: 'string', enum: ['all','relay'] } }, required: ['sessionId','peers','speakingPeerIds','canSpeak','iceServersJson','iceTransportPolicy'] }, errors: { forbidden: { message: 'You cannot join voice.', code: 'FORBIDDEN', id: 'ca31ab3f-1231-4178-94d8-abfaf7740a16' }, notVoice: { message: 'This is not an active voice channel.', code: 'NOT_VOICE_CHANNEL', id: '9155eba4-a234-4f05-875d-2ad5bd9a6459' } } } as const;
export const paramDef = { type: 'object', properties: { communityId: { type: 'string', format: 'misskey:id' }, channelId: { type: 'string', format: 'misskey:id' } }, required: ['communityId','channelId'] } as const;
@Injectable() export default class extends Endpoint<typeof meta, typeof paramDef> { constructor(@Inject(DI.db) private db: DataSource) { super(meta, paramDef, async (ps, me) => { try { return await joinNookCommunityVoice(this.db, ps.communityId, ps.channelId, me.id); } catch (error) { if (error instanceof NookCommunityVoiceError && error.code === 'NOT_VOICE_CHANNEL') throw new ApiError(meta.errors.notVoice); if (error instanceof NookCommunityAccessError || error instanceof NookCommunityChannelError) throw new ApiError(meta.errors.forbidden); throw error; } }); } }
