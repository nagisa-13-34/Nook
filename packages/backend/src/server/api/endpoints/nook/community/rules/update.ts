/*
 * SPDX-FileCopyrightText: syuilo and misskey-project
 * SPDX-License-Identifier: AGPL-3.0-only
 */
import { Inject, Injectable } from '@nestjs/common'; import { DataSource } from 'typeorm'; import { DI } from '@/di-symbols.js'; import { Endpoint } from '@/server/api/endpoint-base.js'; import { requireNookCommunityPermission, NookCommunityAccessError } from '@/nook/community/access.js'; import { updateNookCommunityRule } from '@/nook/community/rules.js'; import { ApiError } from '../../../../error.js';
export const meta = { tags: ['channels'], requireCredential: true, kind: 'write:channels', res: { type: 'object', optional: false, nullable: false, properties: { id: { type: 'string' }, communityId: { type: 'string' }, position: { type: 'number' }, title: { type: 'string' }, body: { type: 'string' } }, required: ['id', 'communityId', 'position', 'title', 'body'] }, errors: { forbidden: { message: 'You cannot manage rules.', code: 'FORBIDDEN', id: '2b665f2a-734b-42b9-b904-32bf96ad6824' }, noSuchRule: { message: 'No such rule.', code: 'NO_SUCH_RULE', id: '18c41800-ef4e-4dd0-b39b-dc693794808f' } } } as const;
export const paramDef = { type: 'object', properties: { communityId: { type: 'string', format: 'misskey:id' }, ruleId: { type: 'string', format: 'misskey:id' }, title: { type: 'string', minLength: 1, maxLength: 128, nullable: true }, body: { type: 'string', minLength: 1, maxLength: 4096, nullable: true }, position: { type: 'integer', minimum: 0, maximum: 10000, nullable: true } }, required: ['communityId', 'ruleId'] } as const;
@Injectable() export default class extends Endpoint<typeof meta, typeof paramDef> { constructor(@Inject(DI.db) private db: DataSource) { super(meta, paramDef, async (ps, me) => { try { await requireNookCommunityPermission(this.db, ps.communityId, me.id, 'rules.manage'); } catch (error) { if (error instanceof NookCommunityAccessError) throw new ApiError(meta.errors.forbidden); throw error; } const rule = await updateNookCommunityRule(this.db, ps.communityId, ps.ruleId, { ...(ps.title != null ? { title: ps.title } : {}), ...(ps.body != null ? { body: ps.body } : {}), ...(ps.position != null ? { position: ps.position } : {}) }); if (rule == null) throw new ApiError(meta.errors.noSuchRule); return rule; }); } }
