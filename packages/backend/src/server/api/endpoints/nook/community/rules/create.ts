/*
 * SPDX-FileCopyrightText: syuilo and misskey-project
 * SPDX-License-Identifier: AGPL-3.0-only
 */
import { Inject, Injectable } from '@nestjs/common'; import { DataSource } from 'typeorm'; import { DI } from '@/di-symbols.js'; import { IdService } from '@/core/IdService.js'; import { Endpoint } from '@/server/api/endpoint-base.js'; import { requireNookCommunityPermission, NookCommunityAccessError } from '@/nook/community/access.js'; import { createNookCommunityRule } from '@/nook/community/rules.js'; import { ApiError } from '../../../../error.js';
export const meta = { tags: ['channels'], requireCredential: true, kind: 'write:channels', res: { type: 'object', optional: false, nullable: false, properties: { id: { type: 'string' }, communityId: { type: 'string' }, position: { type: 'number' }, title: { type: 'string' }, body: { type: 'string' } }, required: ['id', 'communityId', 'position', 'title', 'body'] }, errors: { forbidden: { message: 'You cannot manage rules.', code: 'FORBIDDEN', id: '9be96ccd-c1cb-45b7-ae0c-45f49a21ff5e' } } } as const;
export const paramDef = { type: 'object', properties: { communityId: { type: 'string', format: 'misskey:id' }, title: { type: 'string', minLength: 1, maxLength: 128 }, body: { type: 'string', minLength: 1, maxLength: 4096 }, position: { type: 'integer', minimum: 0, maximum: 10000 } }, required: ['communityId', 'title', 'body'] } as const;
@Injectable() export default class extends Endpoint<typeof meta, typeof paramDef> { constructor(@Inject(DI.db) private db: DataSource, private idService: IdService) { super(meta, paramDef, async (ps, me) => { try { await requireNookCommunityPermission(this.db, ps.communityId, me.id, 'rules.manage'); } catch (error) { if (error instanceof NookCommunityAccessError) throw new ApiError(meta.errors.forbidden); throw error; } return await createNookCommunityRule(this.db, this.idService, ps.communityId, ps.title, ps.body, ps.position ?? 0); }); } }
