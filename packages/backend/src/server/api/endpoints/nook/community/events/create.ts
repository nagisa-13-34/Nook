/*
 * SPDX-FileCopyrightText: syuilo and misskey-project
 * SPDX-License-Identifier: AGPL-3.0-only
 */
import { Inject, Injectable } from '@nestjs/common'; import { DataSource } from 'typeorm'; import { DI } from '@/di-symbols.js'; import { IdService } from '@/core/IdService.js'; import { Endpoint } from '@/server/api/endpoint-base.js'; import { NookAccessService } from '@/nook/policy/NookAccessService.js'; import { requireNookCommunityPermission, NookCommunityAccessError } from '@/nook/community/access.js'; import { createNookCommunityEvent } from '@/nook/community/events.js'; import { NookCommunityReferenceError } from '@/nook/community/references.js'; import { ApiError } from '../../../../error.js';
export const meta = { tags: ['channels'], requireCredential: true, kind: 'write:channels', res: { type: 'object', optional: false, nullable: false, properties: { id: { type: 'string' } }, required: ['id'] }, errors: { forbidden: { message: 'You cannot manage events.', code: 'FORBIDDEN', id: '0d982900-9a6f-4f85-aed3-185d8f201e51' }, voiceDisabled: { message: 'Voice calls are disabled.', code: 'NOOK_VOICE_CALL_DISABLED', id: '91bcc2b8-ae7c-490a-8878-766900866b21', kind: 'permission', httpStatusCode: 403 }, invalidSchedule: { message: 'Invalid event schedule.', code: 'INVALID_SCHEDULE', id: 'd15ca476-7612-47a0-8e40-e8dd95fa9ce5' }, invalidChannel: { message: 'Event channels must belong to this community and match their expected type.', code: 'INVALID_CHANNEL', id: '837094d3-d32e-418c-8dbf-031359fdc25e' } } } as const;
export const paramDef = { type: 'object', properties: { communityId: { type: 'string', format: 'misskey:id' }, title: { type: 'string', minLength: 1, maxLength: 160 }, description: { type: 'string', maxLength: 12000, nullable: true }, location: { type: 'string', maxLength: 256, nullable: true }, startsAt: { type: 'string', format: 'date-time' }, endsAt: { type: 'string', format: 'date-time', nullable: true }, maxAttendees: { type: 'integer', minimum: 1, maximum: 100000, nullable: true }, textChannelId: { type: 'string', format: 'misskey:id', nullable: true }, voiceChannelId: { type: 'string', format: 'misskey:id', nullable: true } }, required: ['communityId','title','startsAt'] } as const;
@Injectable() export default class extends Endpoint<typeof meta, typeof paramDef> { constructor(@Inject(DI.db) private db: DataSource, private idService: IdService, private nookAccessService: NookAccessService) { super(meta, paramDef, async (ps, me) => { try { await requireNookCommunityPermission(this.db, ps.communityId, me.id, 'events.manage'); } catch (error) { if (error instanceof NookCommunityAccessError) throw new ApiError(meta.errors.forbidden); throw error; } if (ps.voiceChannelId != null && !(await this.nookAccessService.isFeatureEnabled('voice_call'))) throw new ApiError(meta.errors.voiceDisabled); const startsAt = new Date(ps.startsAt); const endsAt = ps.endsAt == null ? null : new Date(ps.endsAt); if (endsAt != null && endsAt <= startsAt) throw new ApiError(meta.errors.invalidSchedule); try { const id = await createNookCommunityEvent(this.db, this.idService, { communityId: ps.communityId, creatorId: me.id, title: ps.title, description: ps.description ?? null, location: ps.location ?? null, startsAt, endsAt, maxAttendees: ps.maxAttendees ?? null, textChannelId: ps.textChannelId ?? null, voiceChannelId: ps.voiceChannelId ?? null }); return { id }; } catch (error) { if (error instanceof NookCommunityReferenceError) throw new ApiError(meta.errors.invalidChannel); throw error; } }); } }
